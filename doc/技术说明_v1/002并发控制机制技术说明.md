# 并发控制机制技术说明

## 一、背景与问题

### 1.1 为什么需要并发控制

在高并发场景下，系统面临多种挑战：

- **流量突增**：短时间内大量请求涌入，可能导致系统过载
- **服务雪崩**：某个服务故障导致级联失败，整个系统不可用
- **资源竞争**：多个请求同时操作同一资源，导致数据不一致
- **响应缓慢**：系统负载过高，响应时间急剧增加

### 1.2 并发控制的作用

并发控制机制通过多种策略保护系统：

- **限流**：控制请求速率，防止系统过载
- **熔断**：快速失败，防止级联故障
- **降级**：牺牲非核心功能，保障核心业务
- **分布式锁**：解决并发安全问题，保证数据一致性

### 1.3 本方案解决的痛点

- **无并发控制**：系统无法应对突发流量
- **服务雪崩**：单个服务故障导致整个系统不可用
- **数据不一致**：并发操作导致数据冲突
- **资源耗尽**：高并发下系统资源被耗尽

---

## 二、代码解析

### 2.1 并发控制配置文件

#### 文件位置：`src/config/concurrency-control.ts`

```typescript
import { ConfigService } from '@nestjs/config';

export interface RateLimitConfig {
  enabled: boolean;
  ttl: number;
  limit: number;
  skipSuccessfulRequests: boolean;
  skipFailedRequests: boolean;
}

export interface CircuitBreakerConfig {
  enabled: boolean;
  timeout: number;
  errorThresholdPercentage: number;
  resetTimeout: number;
  rollingCountTimeout: number;
  rollingCountBuckets: number;
}

export interface ServiceDegradationConfig {
  enabled: boolean;
  fallbackEnabled: boolean;
  fallbackTimeout: number;
  maxConcurrentRequests: number;
}

export interface DistributedLockConfig {
  enabled: boolean;
  retryDelay: number;
  retryCount: number;
  lockTimeout: number;
}

export const getConcurrencyControlConfig = (
  configService: ConfigService
): ConcurrencyControlConfig => {
  const nodeEnv = configService.get<string>('NODE_ENV') || 'dev';

  return {
    rateLimit: {
      enabled: configService.get<string>('RATE_LIMIT_ENABLED') !== 'false',
      ttl: configService.get<number>('RATE_LIMIT_TTL') || 60,
      limit: configService.get<number>('RATE_LIMIT_LIMIT') || 100,
      skipSuccessfulRequests:
        configService.get<string>('RATE_LIMIT_SKIP_SUCCESSFUL') === 'true',
      skipFailedRequests: configService.get<string>('RATE_LIMIT_SKIP_FAILED') === 'true',
    },
    circuitBreaker: {
      enabled: configService.get<string>('CIRCUIT_BREAKER_ENABLED') !== 'false',
      timeout: configService.get<number>('CIRCUIT_BREAKER_TIMEOUT') || 10000,
      errorThresholdPercentage:
        configService.get<number>('CIRCUIT_BREAKER_ERROR_THRESHOLD') || 50,
      resetTimeout: configService.get<number>('CIRCUIT_BREAKER_RESET_TIMEOUT') || 30000,
      rollingCountTimeout: configService.get<number>('CIRCUIT_BREAKER_ROLLING_TIMEOUT') || 10000,
      rollingCountBuckets: configService.get<number>('CIRCUIT_BREAKER_ROLLING_BUCKETS') || 10,
    },
    serviceDegradation: {
      enabled: configService.get<string>('SERVICE_DEGRADATION_ENABLED') !== 'false',
      fallbackEnabled: configService.get<string>('SERVICE_FALLBACK_ENABLED') === 'true',
      fallbackTimeout: configService.get<number>('SERVICE_FALLBACK_TIMEOUT') || 5000,
      maxConcurrentRequests: configService.get<number>('SERVICE_MAX_CONCURRENT') || 1000,
    },
    distributedLock: {
      enabled: configService.get<string>('DISTRIBUTED_LOCK_ENABLED') !== 'false',
      retryDelay: configService.get<number>('DISTRIBUTED_LOCK_RETRY_DELAY') || 100,
      retryCount: configService.get<number>('DISTRIBUTED_LOCK_RETRY_COUNT') || 3,
      lockTimeout: configService.get<number>('DISTRIBUTED_LOCK_TIMEOUT') || 30000,
    },
  };
};
```

**关键点解析**：

1. **配置项分离**：将限流、熔断、降级、分布式锁的配置分开管理
2. **环境变量支持**：所有配置项都支持通过环境变量覆盖
3. **默认值合理**：提供合理的默认值，开箱即用
4. **开关控制**：每个功能都有独立的开关，便于灵活控制

**环境变量配置**：

```bash
# 限流配置
RATE_LIMIT_ENABLED=true
RATE_LIMIT_TTL=60
RATE_LIMIT_LIMIT=100
RATE_LIMIT_SKIP_SUCCESSFUL=false
RATE_LIMIT_SKIP_FAILED=false

# 熔断器配置
CIRCUIT_BREAKER_ENABLED=true
CIRCUIT_BREAKER_TIMEOUT=10000
CIRCUIT_BREAKER_ERROR_THRESHOLD=50
CIRCUIT_BREAKER_RESET_TIMEOUT=30000
CIRCUIT_BREAKER_ROLLING_TIMEOUT=10000
CIRCUIT_BREAKER_ROLLING_BUCKETS=10

# 服务降级配置
SERVICE_DEGRADATION_ENABLED=true
SERVICE_FALLBACK_ENABLED=true
SERVICE_FALLBACK_TIMEOUT=5000
SERVICE_MAX_CONCURRENT=1000

# 分布式锁配置
DISTRIBUTED_LOCK_ENABLED=true
DISTRIBUTED_LOCK_RETRY_DELAY=100
DISTRIBUTED_LOCK_RETRY_COUNT=3
DISTRIBUTED_LOCK_TIMEOUT=30000
```

---

### 2.2 限流中间件（令牌桶算法）

#### 文件位置：`src/common/interceptor/rate-limit.interceptor.ts`

```typescript
import { Injectable, NestInterceptor, ExecutionContext, CallHandler, Logger } from '@nestjs/common';
import { Observable, throwError } from 'rxjs';
import { catchError, tap } from 'rxjs/operators';
import { Request } from 'express';
import { CacheService } from '@/common/service/cache.service';
import { getConcurrencyControlConfig, RateLimitConfig } from '@/config/concurrency-control';

interface TokenBucket {
  tokens: number;
  lastRefill: number;
}

@Injectable()
export class RateLimitInterceptor implements NestInterceptor {
  private readonly logger = new Logger(RateLimitInterceptor.name);
  private readonly config: RateLimitConfig;
  private readonly buckets: Map<string, TokenBucket> = new Map();

  constructor(private readonly cacheService: CacheService) {
    this.config = getConcurrencyControlConfig(new (require('@nestjs/config').ConfigService)()).rateLimit;
  }

  intercept(context: ExecutionContext, next: CallHandler): Observable<unknown> {
    if (!this.config.enabled) {
      return next.handle();
    }

    const request = context.switchToHttp().getRequest<Request>();
    const key = this.getRequestKey(request);

    return next.handle().pipe(
      tap(() => {
        if (!this.config.skipSuccessfulRequests) {
          this.recordRequest(key, true);
        }
      }),
      catchError((error) => {
        if (!this.config.skipFailedRequests) {
          this.recordRequest(key, false);
        }
        return throwError(() => error);
      })
    );
  }

  private getRequestKey(request: Request): string {
    const ip = this.getClientIp(request);
    const path = request.route?.path || request.path;
    return `rate-limit:${ip}:${path}`;
  }

  private getClientIp(request: Request): string {
    return (
      (request.headers['x-forwarded-for'] as string)?.split(',')[0]?.trim() ||
      (request.headers['x-real-ip'] as string) ||
      request.connection.remoteAddress ||
      request.socket.remoteAddress ||
      request.ip ||
      'unknown'
    );
  }

  private async recordRequest(key: string, success: boolean): Promise<void> {
    try {
      const now = Date.now();
      let bucket = this.buckets.get(key);

      if (!bucket) {
        bucket = await this.loadBucketFromCache(key);
      }

      if (!bucket) {
        bucket = {
          tokens: this.config.limit,
          lastRefill: now,
        };
      }

      bucket = this.refillBucket(bucket, now);

      if (bucket.tokens > 0) {
        bucket.tokens--;
        this.buckets.set(key, bucket);
        await this.saveBucketToCache(key, bucket);
      } else {
        this.logger.warn(`Rate limit exceeded for key: ${key}`);
        throw new Error('Too Many Requests');
      }
    } catch (error) {
      if (error instanceof Error && error.message === 'Too Many Requests') {
        throw error;
      }
      this.logger.error(`Error recording request: ${error.message}`);
    }
  }

  private refillBucket(bucket: TokenBucket, now: number): TokenBucket {
    const timePassed = now - bucket.lastRefill;
    const refillAmount = Math.floor((timePassed / (this.config.ttl * 1000)) * this.config.limit);

    if (refillAmount > 0) {
      bucket.tokens = Math.min(this.config.limit, bucket.tokens + refillAmount);
      bucket.lastRefill = now;
    }

    return bucket;
  }

  private async loadBucketFromCache(key: string): Promise<TokenBucket | null> {
    try {
      const cached = await this.cacheService.get<TokenBucket>(key);
      return cached;
    } catch (error) {
      return null;
    }
  }

  private async saveBucketToCache(key: string, bucket: TokenBucket): Promise<void> {
    try {
      await this.cacheService.set(key, bucket, this.config.ttl);
    } catch (error) {
      this.logger.error(`Error saving bucket to cache: ${error.message}`);
    }
  }
}
```

**关键点解析**：

1. **令牌桶算法**：
   - 桶中有固定数量的令牌（limit）
   - 每个请求消耗一个令牌
   - 令牌按固定速率补充（refill）
   - 令牌耗尽时拒绝请求

2. **分布式支持**：
   - 使用Redis存储令牌桶状态
   - 支持多实例共享限流状态
   - 本地缓存优化性能

3. **IP识别**：
   - 优先使用 `x-forwarded-for` 头
   - 支持代理服务器场景
   - 降级使用连接地址

4. **灵活配置**：
   - 可配置是否跳过成功/失败请求
   - 可配置TTL和限流阈值
   - 支持全局开关

**令牌桶算法原理**：

```
初始状态：桶中有100个令牌
请求1：消耗1个令牌，剩余99个
请求2：消耗1个令牌，剩余98个
...
请求100：消耗1个令牌，剩余0个
请求101：令牌耗尽，拒绝请求
1秒后：补充100个令牌，恢复到100个
```

---

### 2.3 熔断器服务

#### 文件位置：`src/common/service/circuit-breaker.service.ts`

```typescript
import { Injectable, Logger } from '@nestjs/common';
import { getConcurrencyControlConfig, CircuitBreakerConfig } from '@/config/concurrency-control';

export enum CircuitState {
  CLOSED = 'CLOSED',
  OPEN = 'OPEN',
  HALF_OPEN = 'HALF_OPEN',
}

export interface CircuitStats {
  successCount: number;
  failureCount: number;
  totalRequests: number;
  errorRate: number;
  lastFailureTime: number;
  lastSuccessTime: number;
}

export interface CircuitBreakerState {
  state: CircuitState;
  stats: CircuitStats;
  openedAt: number;
  nextAttemptAt: number;
}

@Injectable()
export class CircuitBreakerService {
  private readonly logger = new Logger(CircuitBreakerService.name);
  private readonly config: CircuitBreakerConfig;
  private readonly circuits: Map<string, CircuitBreakerState> = new Map();

  constructor() {
    this.config = getConcurrencyControlConfig(new (require('@nestjs/config').ConfigService)()).circuitBreaker;
  }

  async execute<T>(
    key: string,
    fn: () => Promise<T>,
    fallback?: () => Promise<T>
  ): Promise<T> {
    if (!this.config.enabled) {
      return fn();
    }

    const circuit = this.getOrCreateCircuit(key);

    if (circuit.state === CircuitState.OPEN) {
      if (Date.now() >= circuit.nextAttemptAt) {
        this.transitionToHalfOpen(key, circuit);
      } else {
        this.logger.warn(`Circuit breaker is OPEN for key: ${key}`);
        if (fallback) {
          return fallback();
        }
        throw new Error('Circuit breaker is OPEN');
      }
    }

    const startTime = Date.now();
    try {
      const result = await Promise.race([
        fn(),
        this.createTimeoutPromise(this.config.timeout),
      ]);

      const duration = Date.now() - startTime;
      this.recordSuccess(key, duration);
      return result;
    } catch (error) {
      const duration = Date.now() - startTime;
      this.recordFailure(key, duration, error as Error);

      if (circuit.state === CircuitState.HALF_OPEN) {
        this.transitionToOpen(key, circuit);
      }

      if (fallback) {
        return fallback();
      }

      throw error;
    }
  }

  private transitionToOpen(key: string, circuit: CircuitBreakerState): void {
    circuit.state = CircuitState.OPEN;
    circuit.openedAt = Date.now();
    circuit.nextAttemptAt = Date.now() + this.config.resetTimeout;
    this.logger.warn(`Circuit breaker OPENED for key: ${key}`);
  }

  private transitionToHalfOpen(key: string, circuit: CircuitBreakerState): void {
    circuit.state = CircuitState.HALF_OPEN;
    this.logger.info(`Circuit breaker transitioned to HALF_OPEN for key: ${key}`);
  }

  private transitionToClosed(key: string, circuit: CircuitBreakerState): void {
    circuit.state = CircuitState.CLOSED;
    circuit.stats = {
      successCount: 0,
      failureCount: 0,
      totalRequests: 0,
      errorRate: 0,
      lastFailureTime: 0,
      lastSuccessTime: 0,
    };
    this.logger.info(`Circuit breaker CLOSED for key: ${key}`);
  }
}
```

**关键点解析**：

1. **三态模型**：
   - **CLOSED（关闭）**：正常状态，请求正常通过
   - **OPEN（打开）**：熔断状态，直接拒绝请求
   - **HALF_OPEN（半开）**：尝试恢复，允许少量请求测试

2. **状态转换**：
   ```
   CLOSED → OPEN: 错误率超过阈值
   OPEN → HALF_OPEN: 超过重置时间
   HALF_OPEN → CLOSED: 测试请求成功
   HALF_OPEN → OPEN: 测试请求失败
   ```

3. **超时保护**：
   - 每个请求都有超时限制
   - 超时视为失败
   - 防止请求阻塞

4. **降级支持**：
   - 熔断时可以执行降级逻辑
   - 提供fallback函数
   - 保证服务可用性

**熔断器工作流程**：

```
初始状态：CLOSED
请求1：成功 → successCount++
请求2：成功 → successCount++
...
请求N：失败 → failureCount++
错误率计算：failureCount / totalRequests
错误率 > 50% → 状态变为OPEN
OPEN状态：所有请求直接返回错误
30秒后：状态变为HALF_OPEN
HALF_OPEN状态：允许少量请求测试
测试成功 → 状态变为CLOSED
测试失败 → 状态变为OPEN
```

---

### 2.4 服务降级服务

#### 文件位置：`src/common/service/service-degradation.service.ts`

```typescript
import { Injectable, Logger, OnModuleInit } from '@nestjs/common';
import { getConcurrencyControlConfig, ServiceDegradationConfig } from '@/config/concurrency-control';

export interface DegradationLevel {
  level: number;
  name: string;
  description: string;
}

export interface ServiceStatus {
  serviceName: string;
  isDegraded: boolean;
  degradationLevel: number;
  currentRequests: number;
  lastDegradedAt: number;
  lastRecoveredAt: number;
}

export interface FallbackHandler<T> {
  (error: Error): Promise<T>;
}

@Injectable()
export class ServiceDegradationService implements OnModuleInit {
  private readonly logger = new Logger(ServiceDegradationService.name);
  private readonly config: ServiceDegradationConfig;
  private readonly serviceStatus: Map<string, ServiceStatus> = new Map();
  private readonly fallbackHandlers: Map<string, FallbackHandler<unknown>> = new Map();
  private readonly degradationLevels: DegradationLevel[] = [
    { level: 0, name: 'NORMAL', description: '正常服务' },
    { level: 1, name: 'MINIMAL', description: '最小化服务' },
    { level: 2, name: 'EMERGENCY', description: '紧急模式' },
  ];
  private currentLevel = 0;

  constructor() {
    this.config = getConcurrencyControlConfig(new (require('@nestjs/config').ConfigService)()).serviceDegradation;
  }

  async execute<T>(
    serviceName: string,
    fn: () => Promise<T>,
    fallback?: FallbackHandler<T>
  ): Promise<T> {
    if (!this.config.enabled) {
      return fn();
    }

    const status = this.getOrCreateServiceStatus(serviceName);

    if (status.isDegraded && this.config.fallbackEnabled) {
      this.logger.warn(`Service ${serviceName} is degraded, using fallback`);
      const handler = this.fallbackHandlers.get(serviceName) || fallback;
      if (handler) {
        try {
          return await Promise.race([
            handler(new Error('Service degraded')),
            this.createFallbackTimeout(),
          ]);
        } catch (error) {
          this.logger.error(`Fallback failed for service ${serviceName}:`, error);
          throw error;
        }
      }
    }

    status.currentRequests++;

    try {
      const result = await fn();
      status.currentRequests--;
      this.checkServiceRecovery(serviceName, status);
      return result;
    } catch (error) {
      status.currentRequests--;
      this.checkServiceDegradation(serviceName, status);
      throw error;
    }
  }

  degrade(serviceName: string, level: number = 1): void {
    const status = this.getOrCreateServiceStatus(serviceName);
    status.isDegraded = true;
    status.degradationLevel = level;
    status.lastDegradedAt = Date.now();
    this.logger.warn(`Service ${serviceName} degraded to level ${level}`);
  }

  recover(serviceName: string): void {
    const status = this.serviceStatus.get(serviceName);
    if (status) {
      status.isDegraded = false;
      status.degradationLevel = 0;
      status.lastRecoveredAt = Date.now();
      this.logger.info(`Service ${serviceName} recovered`);
    }
  }

  setGlobalDegradationLevel(level: number): void {
    if (level < 0 || level >= this.degradationLevels.length) {
      throw new Error(`Invalid degradation level: ${level}`);
    }
    this.currentLevel = level;
    this.logger.warn(`Global degradation level set to ${this.degradationLevels[level].name}`);

    for (const [serviceName, status] of this.serviceStatus) {
      if (level > 0 && !status.isDegraded) {
        this.degrade(serviceName, level);
      } else if (level === 0 && status.isDegraded) {
        this.recover(serviceName);
      }
    }
  }
}
```

**关键点解析**：

1. **三级降级**：
   - **NORMAL（0级）**：正常服务，所有功能可用
   - **MINIMAL（1级）**：最小化服务，只保留核心功能
   - **EMERGENCY（2级）**：紧急模式，只提供最基本功能

2. **自动降级**：
   - 当前请求数超过阈值时自动降级
   - 负载降低后自动恢复
   - 避免手动干预

3. **降级策略**：
   - 注册fallback处理器
   - 降级时执行fallback
   - 保证服务可用性

4. **全局控制**：
   - 支持设置全局降级级别
   - 统一管理所有服务
   - 快速响应系统故障

**降级策略示例**：

```
正常服务（0级）：
- 用户注册
- 用户登录
- 数据查询
- 文件上传
- 邮件发送

最小化服务（1级）：
- 用户登录
- 数据查询
- 文件上传（降级为本地存储）
- 邮件发送（降级为异步队列）

紧急模式（2级）：
- 用户登录
- 数据查询（只读）
- 文件上传（禁用）
- 邮件发送（禁用）
```

---

### 2.5 分布式锁服务

#### 文件位置：`src/common/service/distributed-lock.service.ts`

```typescript
import { Injectable, Logger, OnModuleDestroy } from '@nestjs/common';
import { CacheService } from '@/common/service/cache.service';
import { getConcurrencyControlConfig, DistributedLockConfig } from '@/config/concurrency-control';
import { v4 as uuidv4 } from 'uuid';

export interface LockOptions {
  timeout?: number;
  retryDelay?: number;
  retryCount?: number;
}

export interface LockResult {
  success: boolean;
  lockId?: string;
  error?: Error;
}

@Injectable()
export class DistributedLockService implements OnModuleDestroy {
  private readonly logger = new Logger(DistributedLockService.name);
  private readonly config: DistributedLockConfig;
  private readonly activeLocks: Map<string, string> = new Map();

  constructor(private readonly cacheService: CacheService) {
    this.config = getConcurrencyControlConfig(new (require('@nestjs/config').ConfigService)()).distributedLock;
  }

  async acquireLock(
    key: string,
    options: LockOptions = {}
  ): Promise<LockResult> {
    if (!this.config.enabled) {
      return { success: true };
    }

    const lockId = uuidv4();
    const timeout = options.timeout || this.config.lockTimeout;
    const retryDelay = options.retryDelay || this.config.retryDelay;
    const retryCount = options.retryCount || this.config.retryCount;

    for (let attempt = 0; attempt <= retryCount; attempt++) {
      try {
        const success = await this.tryAcquireLock(key, lockId, timeout);

        if (success) {
          this.activeLocks.set(key, lockId);
          this.logger.debug(`Lock acquired for key: ${key}, lockId: ${lockId}`);
          return { success: true, lockId };
        }

        if (attempt < retryCount) {
          await this.sleep(retryDelay);
        }
      } catch (error) {
        this.logger.error(
          `Error acquiring lock for key ${key} (attempt ${attempt + 1}/${retryCount + 1}):`,
          error
        );
      }
    }

    this.logger.warn(`Failed to acquire lock for key: ${key} after ${retryCount + 1} attempts`);
    return {
      success: false,
      error: new Error(`Failed to acquire lock after ${retryCount + 1} attempts`),
    };
  }

  async releaseLock(key: string, lockId: string): Promise<boolean> {
    if (!this.config.enabled) {
      return true;
    }

    try {
      const activeLockId = this.activeLocks.get(key);

      if (activeLockId !== lockId) {
        this.logger.warn(`Attempted to release lock with invalid lockId for key: ${key}`);
        return false;
      }

      const success = await this.tryReleaseLock(key, lockId);

      if (success) {
        this.activeLocks.delete(key);
        this.logger.debug(`Lock released for key: ${key}, lockId: ${lockId}`);
        return true;
      }

      return false;
    } catch (error) {
      this.logger.error(`Error releasing lock for key ${key}:`, error);
      return false;
    }
  }

  async executeWithLock<T>(
    key: string,
    fn: () => Promise<T>,
    options?: LockOptions
  ): Promise<T> {
    const lockResult = await this.acquireLock(key, options);

    if (!lockResult.success) {
      throw new Error(
        `Failed to acquire lock for key: ${key}. ${lockResult.error?.message || ''}`
      );
    }

    try {
      return await fn();
    } finally {
      if (lockResult.lockId) {
        await this.releaseLock(key, lockResult.lockId);
      }
    }
  }
}
```

**关键点解析**：

1. **Redis实现**：
   - 使用Redis的SETNX命令实现锁
   - 设置过期时间防止死锁
   - 使用UUID保证锁的唯一性

2. **重试机制**：
   - 获取锁失败时自动重试
   - 可配置重试次数和延迟
   - 提高锁获取成功率

3. **锁验证**：
   - 释放锁时验证lockId
   - 防止误释放其他实例的锁
   - 保证锁的安全性

4. **自动释放**：
   - 应用关闭时释放所有锁
   - 使用finally确保释放
   - 防止锁泄漏

**分布式锁使用场景**：

1. **库存扣减**：
   ```typescript
   await lockService.executeWithLock('inventory:product:123', async () => {
     const product = await getProduct(123);
     if (product.stock > 0) {
       product.stock--;
       await updateProduct(product);
     }
   });
   ```

2. **订单创建**：
   ```typescript
   await lockService.executeWithLock('order:user:456', async () => {
     const order = await createOrder(userId, items);
     await deductStock(items);
     await sendNotification(order);
   });
   ```

3. **缓存更新**：
   ```typescript
   await lockService.executeWithLock('cache:user:789', async () => {
     const data = await fetchData(userId);
     await cacheService.set(`user:${userId}`, data);
   });
   ```

---

## 三、示例代码

### 3.1 使用限流中间件

```typescript
import { Controller, Get, UseInterceptors } from '@nestjs/common';
import { RateLimitInterceptor } from '@/common/interceptor/rate-limit.interceptor';

@Controller('api')
export class ApiController {
  @Get('data')
  @UseInterceptors(RateLimitInterceptor)
  async getData() {
    return { message: 'Data fetched successfully' };
  }
}
```

### 3.2 使用熔断器

```typescript
import { Controller, Get } from '@nestjs/common';
import { CircuitBreakerService } from '@/common/service/circuit-breaker.service';

@Controller('api')
export class ApiController {
  constructor(private readonly circuitBreakerService: CircuitBreakerService) {}

  @Get('external')
  async callExternalAPI() {
    return await this.circuitBreakerService.execute(
      'external-api',
      async () => {
        return await fetchExternalData();
      },
      async () => {
        return { message: 'Service unavailable, using cached data' };
      }
    );
  }
}
```

### 3.3 使用服务降级

```typescript
import { Controller, Get } from '@nestjs/common';
import { ServiceDegradationService } from '@/common/service/service-degradation.service';

@Controller('api')
export class ApiController {
  constructor(private readonly degradationService: ServiceDegradationService) {
    this.degradationService.registerFallback('email-service', async (error) => {
      return { message: 'Email service degraded, queued for later' };
    });
  }

  @Get('send-email')
  async sendEmail() {
    return await this.degradationService.execute(
      'email-service',
      async () => {
        return await sendEmail();
      }
    );
  }
}
```

### 3.4 使用分布式锁

```typescript
import { Controller, Post, Body } from '@nestjs/common';
import { DistributedLockService } from '@/common/service/distributed-lock.service';

@Controller('api')
export class ApiController {
  constructor(private readonly lockService: DistributedLockService) {}

  @Post('order')
  async createOrder(@Body() orderData: any) {
    return await this.lockService.executeWithLock(
      `order:user:${orderData.userId}`,
      async () => {
        return await createOrder(orderData);
      }
    );
  }
}
```

### 3.5 组合使用装饰器

```typescript
import { Controller, Get, Post, Body } from '@nestjs/common';
import { CircuitBreaker } from '@/common/decorators/circuit-breaker.decorator';
import { ServiceDegradation } from '@/common/decorators/service-degradation.decorator';
import { DistributedLock } from '@/common/decorators/distributed-lock.decorator';
import { UseInterceptors } from '@nestjs/common';
import { ConcurrencyControlInterceptor } from '@/common/interceptor/concurrency-control.interceptor';

@Controller('api')
@UseInterceptors(ConcurrencyControlInterceptor)
export class ApiController {
  @Get('data')
  @CircuitBreaker({ key: 'data-api' })
  @ServiceDegradation({ serviceName: 'data-service' })
  async getData() {
    return await fetchData();
  }

  @Post('order')
  @DistributedLock({ key: 'order-lock' })
  @CircuitBreaker({ key: 'order-api' })
  async createOrder(@Body() orderData: any) {
    return await createOrder(orderData);
  }
}
```

---

## 四、可用场景

### 4.1 限流场景

1. **API接口限流**：防止恶意请求和DDoS攻击
2. **用户行为限流**：限制单个用户的操作频率
3. **资源保护限流**：保护稀缺资源不被过度消耗

**何时使用**：
- 需要控制请求速率时
- 防止系统过载时
- 保护关键资源时

### 4.2 熔断场景

1. **外部API调用**：调用第三方服务时防止级联故障
2. **数据库操作**：数据库压力过大时快速失败
3. **微服务调用**：服务间调用时防止雪崩

**何时使用**：
- 调用不稳定的外部服务时
- 需要快速失败时
- 防止级联故障时

### 4.3 降级场景

1. **非核心功能**：评论、点赞等可以暂时关闭
2. **资源密集型功能**：图片处理、文件上传等
3. **第三方服务**：支付、短信等可以降级为队列

**何时使用**：
- 系统负载过高时
- 部分服务故障时
- 需要保障核心业务时

### 4.4 分布式锁场景

1. **库存管理**：防止超卖
2. **订单创建**：防止重复订单
3. **缓存更新**：防止缓存击穿

**何时使用**：
- 需要保证数据一致性时
- 多个实例同时操作同一资源时
- 需要互斥访问时

---

## 五、注意事项

### 5.1 限流注意事项

1. **令牌桶配置**：
   - limit不宜过小，否则影响正常用户
   - ttl不宜过大，否则限流效果不明显
   - 建议根据实际业务调整

2. **IP识别**：
   - 注意代理服务器的影响
   - 考虑使用用户ID替代IP
   - 防止IP伪造攻击

3. **降级策略**：
   - 限流后应返回友好的错误信息
   - 提供重试建议
   - 记录限流日志

### 5.2 熔断注意事项

1. **阈值设置**：
   - errorThresholdPercentage不宜过低
   - resetTimeout不宜过短
   - 需要根据实际业务调整

2. **Fallback设计**：
   - Fallback应该简单可靠
   - 避免在Fallback中调用可能失败的服务
   - 考虑使用缓存数据

3. **监控告警**：
   - 监控熔断器状态
   - 熔断时及时告警
   - 定期检查熔断原因

### 5.3 降级注意事项

1. **降级级别**：
   - 明确定义每个级别的可用功能
   - 降级后应有明确的提示
   - 恢复后应及时通知用户

2. **Fallback实现**：
   - Fallback应该快速响应
   - 避免Fallback本身成为瓶颈
   - 考虑使用本地缓存

3. **自动恢复**：
   - 设置合理的恢复条件
   - 避免频繁降级/恢复
   - 监控降级状态

### 5.4 分布式锁注意事项

1. **锁超时**：
   - timeout应该大于业务执行时间
   - 避免锁提前释放
   - 考虑使用锁续期

2. **锁释放**：
   - 确保锁一定会被释放
   - 使用finally块
   - 处理异常情况

3. **锁粒度**：
   - 锁的粒度不宜过大
   - 避免锁竞争
   - 考虑分段锁

---

## 六、性能优化

### 6.1 限流优化

1. **本地缓存**：使用内存缓存减少Redis访问
2. **批量操作**：批量更新令牌桶状态
3. **异步处理**：异步记录请求

### 6.2 熔断优化

1. **滑动窗口**：使用滑动窗口统计错误率
2. **状态缓存**：缓存熔断器状态
3. **快速失败**：OPEN状态直接返回错误

### 6.3 降级优化

1. **预热Fallback**：提前准备Fallback数据
2. **本地缓存**：使用本地缓存存储Fallback结果
3. **异步降级**：异步执行降级逻辑

### 6.4 分布式锁优化

1. **锁续期**：实现锁自动续期
2. **锁分段**：使用分段锁减少竞争
3. **锁池化**：复用锁连接

---

## 七、监控与告警

### 7.1 监控指标

1. **限流指标**：
   - 限流触发次数
   - 限流拒绝率
   - 令牌桶使用率

2. **熔断指标**：
   - 熔断器状态
   - 熔断触发次数
   - 错误率变化

3. **降级指标**：
   - 降级服务数量
   - 降级级别变化
   - Fallback调用次数

4. **分布式锁指标**：
   - 锁获取成功率
   - 锁等待时间
   - 锁持有时间

### 7.2 告警规则

1. **限流告警**：
   - 限流拒绝率超过10%
   - 某个IP限流次数过多

2. **熔断告警**：
   - 熔断器打开
   - 熔断器频繁切换

3. **降级告警**：
   - 服务降级
   - 降级级别提升

4. **分布式锁告警**：
   - 锁获取失败率过高
   - 锁等待时间过长

---

## 八、最佳实践

### 8.1 组合使用

1. **限流 + 熔断**：先限流，再熔断
2. **熔断 + 降级**：熔断后降级
3. **分布式锁 + 降级**：锁失败时降级

### 8.2 配置建议

1. **开发环境**：
   - 限流：limit=1000, ttl=60
   - 熔断：errorThreshold=80, timeout=30000
   - 降级：maxConcurrent=5000

2. **测试环境**：
   - 限流：limit=500, ttl=60
   - 熔断：errorThreshold=60, timeout=15000
   - 降级：maxConcurrent=2000

3. **生产环境**：
   - 限流：limit=100, ttl=60
   - 熔断：errorThreshold=50, timeout=10000
   - 降级：maxConcurrent=1000

### 8.3 测试策略

1. **单元测试**：测试各个组件的功能
2. **集成测试**：测试组件间的协作
3. **压力测试**：测试高并发场景
4. **故障测试**：测试故障恢复能力

---

## 九、总结

并发控制机制是高并发系统的核心保障，通过限流、熔断、降级、分布式锁等多种策略，可以有效提升系统的稳定性和可用性。本方案提供了完整的实现和配置，可以根据实际业务需求灵活调整。

### 核心优势

1. **全面性**：覆盖限流、熔断、降级、分布式锁
2. **灵活性**：支持多种配置和组合使用
3. **可靠性**：基于Redis实现，支持分布式
4. **易用性**：提供装饰器和拦截器，使用简单

### 后续优化

1. **性能优化**：进一步优化性能
2. **功能扩展**：支持更多算法和策略
3. **监控完善**：提供更丰富的监控指标
4. **文档完善**：提供更详细的使用文档
